<div class = "log">
  <h2>비밀번호 찾기</h2>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>

    <div class="field">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true %>
    </div>

    <div class="actions">
      <%= f.submit "새로운 비밀번호를 보내주세요!" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
<%= devise_error_messages! %>
