<div class = "log">
  <p> 안녕하세요! <%= @email %>님!</p>

  <% if @resource.try(:unconfirmed_email?) %>
    <p>귀하의 이메일이 <%= @resource.unconfirmed_email %>로 변경되었음을 알려드립니다.</p>
  <% else %>
    <p>귀하의 이메일이 <%= @resource.email %>로 변경되었음을 알려드립니다.</p>
  <% end %>
</div>
